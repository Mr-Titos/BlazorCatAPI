@page "/"
@inject NavigationManager NavigationManager;

@code {
    // Let this redirect in a case of captcha need or something
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo("/home");
    }
}