@page "/home"

@inject CatService CatService
@inject NavigationManager NavigationManager

<PageTitle>Home</PageTitle>

<CatComponent Cat="CatSelected"></CatComponent>

@code {
    private Cat CatSelected { get; set; }

    protected override void OnInitialized()
    {
        CatSelected = Task.Run(() => CatService.SearchCats()).Result.FirstOrDefault() ?? throw new Exception("No cat retrieved");
    }

}